/**
 * Provides the basic interface to store/load data from/to the history.
 */
class CalculationHistory {
    /**
     * Loads the data from the underlying storage.
     */
    load() { }

    /**
     * Saves the data into the underlying storage.
     */
    store() { }
}

module.exports = CalculationHistory;